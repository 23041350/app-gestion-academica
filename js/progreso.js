// progreso.js - Lógica de progreso y estadísticas

// Renderizar gráficas (no se necesita mucho porque los datos ya están en HTML)
// Pero podemos agregar interactividad si es necesario

document.addEventListener('DOMContentLoaded', () => {
  // Aquí se pueden agregar animaciones o actualizaciones dinámicas
  console.log('Pantalla de progreso cargada');
});
